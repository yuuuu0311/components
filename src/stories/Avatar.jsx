import React from "react";
import PropTypes from "prop-types";
import { useState } from "react";

export const Avatar = ({ isOnline }) => {
    return <div>Avatar</div>;
};

Avatar.propTypes = {
    isOnline: PropTypes.bool,
};
